{%- form 'product', product, id: form_id, class: 'product-single__form' -%}

  {%- if shop.taxes_included or shop.shipping_policy.body != blank -%}
    <div class="product__policies rte">
      {%- if shop.taxes_included -%}
        {{ 'products.product.include_taxes' | t }}
      {%- endif -%}
      {%- if shop.shipping_policy.body != blank -%}
        {{ 'products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
      {%- endif -%}
    </div>
  {%- endif -%}

  {%- liquid
    assign enable_dynamic_buttons = false
    if show_dynamic_checkout and template != 'product.preorder'
      assign enable_dynamic_buttons = true
    endif
  -%}

  {%- if enable_dynamic_buttons -%}
    <div class="payment-buttons">
  {%- endif -%}

    {%- liquid
      assign default_text = 'products.product.add_to_cart' | t
      assign button_text = 'products.product.add_to_cart' | t
      if template contains 'preorder'
        assign default_text = 'products.product.preorder' | t
        assign button_text = 'products.product.preorder' | t
      endif
      unless current_variant.available
        assign button_text = 'products.product.sold_out' | t
      endunless
    -%}


    {% comment %} pre-order date for dispatch for any products not in Gempages - am {% endcomment %}
    {% assign current_variant = product.selected_or_first_available_variant %}

    {%- for variant in product.variants -%}
      {% if variant == current_variant and variant.available %}
        {% assign date-today =  "today" | date: "%s" %}
        {% if metafield-date != empty %}
          {% assign metafield-date =  variant.metafields.custom.pre_order_date  | date: "%s" %}
        {% endif  %}
    
        {% if metafield-date != empty %}
          {% if metafield-date > date-today %}
            {% assign variant-date = metafield-date | date:  "%d/%m" %}
            <div data-variant-date="variant-date" id="preorder-pdp-date">
              AAA - Order today for dispatch by {{variant-date}}
            </div>
          {% else %}
            <div data-variant-date="variant-date" id="preorder-pdp-date"></div>
          {% endif  %}
        {% else %}
          <div data-variant-date="variant-date" id="preorder-pdp-date"></div>
        {% endif  %}
      {% endif  %}
    {%- endfor -%}


    {%- for variant in product.variants -%}
      {% if variant == current_variant %}
        {% assign date-today = "today" | date: "%s" %}
        {% assign metafield-date =  variant.metafields.custom.pre_order_date  | date: "%s" %}

        {% if metafield-date > date-today %}
          <div id="preorder-pdp-msg">We will fulfill the item as soon as it becomes available</div>
        {% endif  %}
      {% endif  %}
    {%- endfor variant -%}
  

    <style>
      #preorder-pdp-date,
      #preorder-pdp-msg {
        text-align: center;
        padding: 3px 6px;
        margin: 5px 0 10px;
        display: block;
        clear: both;
      }

      #preorder-pdp-date {
        font-size: 15px;
        /*font-size: 25px;*/
        color: #4047ba;
        font-weight: 600;
      }
      #preorder-pdp-msg {
        font-size: 12px;
        /*font-size: 25px;*/
      }
    </style>

    <button
      {% if product.empty? %}type="button"{% else %}type="submit"{% endif %}
      name="add"
      data-add-to-cart
      class="btn btn--full add-to-cart{% if enable_dynamic_buttons and product.selling_plan_groups == empty %} btn--secondary{% endif %}"
      {% unless current_variant.available %} disabled="disabled"{% endunless %}>
      <span data-add-to-cart-text data-default-text="{{ default_text }}">
        {{ button_text }}
      </span>
    </button>

    {%- if enable_dynamic_buttons -%}
      {{ form | payment_button }}
    {%- endif -%}

  {%- if enable_dynamic_buttons -%}
    </div>
  {%- endif -%}

  <div class="shopify-payment-terms product__policies">{{ form | payment_terms }}</div>

  <select name="id" data-product-select class="product-single__variants no-js">
    {%- for variant in product.variants -%}
      {%- if variant.available -%}
        <option {% if variant == product.selected_or_first_available_variant %}
          selected="selected"{% endif %}
          value="{{ variant.id }}">
          {{ variant.title }} - {{ variant.price | money_with_currency }}
        </option>
      {%- else -%}
        <option disabled="disabled">
          {{ variant.title }} - {{ 'products.product.sold_out' | t }}
        </option>
      {%- endif -%}
    {%- endfor -%}
  </select>
{%- endform -%}
